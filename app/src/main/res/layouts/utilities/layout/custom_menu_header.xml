<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/menuHeader"
    android:layout_width="match_parent"
    android:layout_height="@dimen/nav_bar_height"
    android:background="@color/transparent"
    android:orientation="horizontal">

    <!--Menu button-->
    <RelativeLayout
        android:id="@+id/burgerLayout"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentStart="true"
        android:padding="10dp">

        <ImageButton
            android:id="@+id/btnMenu"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_centerVertical="true"
            android:background="@android:color/transparent"
            android:contentDescription="@string/imgHint"
            android:src="@drawable/img_header_menu" />

        <ImageButton
            android:id="@+id/btnBack"
            android:layout_width="30dp"
            android:layout_height="40dp"
            android:layout_centerVertical="true"
            android:background="@android:color/transparent"
            android:contentDescription="@string/imgHint"
            android:tint="@color/colorPrimaryDark"
            android:src="@drawable/icon_menu_back" />

    </RelativeLayout>

    <!-- Menu Title -->
    <TextView
        android:id="@+id/lblTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_marginStart="50dp"
        android:padding="10dp"
        android:text="@string/menu_home"
        android:textAppearance="@style/CustomFont.Regular"
        android:textColor="@color/colorPrimary"
        android:textSize="@dimen/custom_header_font_size" />

    <TextView
        android:id="@+id/textView"
        android:layout_width="match_parent"
        android:layout_height="2dp"
        android:layout_alignParentBottom="true"
        android:background="@color/colorPrimary"
        android:text="TextView" />

</RelativeLayout>
